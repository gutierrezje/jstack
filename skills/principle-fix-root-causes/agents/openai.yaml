interface:
  display_name: "Principle Fix Root Causes"
  short_description: "Codex-native pstack workflow: principle-fix-root-causes"
  default_prompt: "Use $principle-fix-root-causes for this task."
policy:
  allow_implicit_invocation: false
